<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm px-3">
  <div class="container-fluid">

    <!-- BRAND -->
    <%= link_to root_path, class: "navbar-brand fw-bold d-flex align-items-center gap-2" do %>
      📚 <span>Bookify</span>
    <% end %>

    <!-- TOGGLER -->
    <button class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#mainNavbar">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- NAVBAR CONTENT -->
    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-2">

  <!-- ALL BOOKS BUTTON 
        <li class="nav-item">
          <%= link_to "All Books",
              books_path,
              class: "btn btn-outline-secondary btn-sm" %>
        </li>
  -->

        <!-- FILTER DROPDOWN -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle d-flex align-items-center gap-1"
             href="#"
             role="button"
             data-bs-toggle="dropdown"
             aria-expanded="false">
            🔍 <span>Filter</span>
          </a>

          <ul class="dropdown-menu dropdown-menu-end shadow-sm">
            <li>
              <%= link_to "📖 All Books",
                  books_path,
                  class: "dropdown-item #{'active' if params[:user_id].blank? && params[:filter].blank?}" %>
            </li>

            <% if user_signed_in? %>
              <li>
                <%= link_to "✍️ My Publishes",
                    books_path(filter: "my_books"),
                    class: "dropdown-item #{'active' if params[:filter] == 'my_books'}" %>
              </li>
            <% end %>

            <!-- DIVIDER -->
            <li><hr class="dropdown-divider"></li>

            <!-- LIST USERS TO FILTER BY -->
            <% User.order(:name).each do |user| %>
              <li>
                <%= link_to "👤 #{user.name.presence || user.email}",
                    books_path(user_id: user.id),
                    class: "dropdown-item #{'active' if params[:user_id] == user.id.to_s}" %>
              </li>
            <% end %>
          </ul>
        </li>

        <!-- CHAT BUTTON -->
        <!-- Only shows if a user is currently selected via the filter -->
        <% if params[:user_id].present? %>
          <% target_user = User.find_by(id: params[:user_id]) %>
          <% if target_user && user_signed_in? %>
            <li class="nav-item">
              <%= link_to "Chat with #{target_user.name.presence || 'User'}",
                  user_path(target_user),
                  class: "btn btn-primary btn-sm" %>
            </li>
          <% end %>
        <% end %>

        <% if user_signed_in? %>

          <li class="nav-item text-light small px-3">
            Signed_in as <%= current_user.name.presence || current_user.email %>
          </li>

          <li class="nav-item">
            <%= button_to "Logout",
                destroy_user_session_path,
                method: :delete,
                class: "btn btn-danger btn-sm" %>
          </li>

        <% else %>

          <li class="nav-item">
            <%= link_to "Login",
                new_user_session_path,
                class: "btn btn-outline-light btn-sm" %>
          </li>

          <li class="nav-item">
            <%= link_to "Sign up",
                new_user_registration_path,
                class: "btn btn-success btn-sm" %>
          </li>

        <% end %>

      </ul>
    </div>
  </div>
</nav>